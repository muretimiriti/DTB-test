# =============================================================================
# namespace.yaml — Banking application namespace
# Apply first: kubectl apply -f manifests/k8s/namespace.yaml
# =============================================================================
apiVersion: v1
kind: Namespace
metadata:
  name: banking
  labels:
    app.kubernetes.io/part-of: dtb-banking-portal
    environment: production
