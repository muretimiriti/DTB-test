# THIS FILE IS INTENTIONALLY EMPTY.
#
# The mongodb-secret Kubernetes Secret is created and managed by
# ExternalSecret (mongodb/external-secret.yaml), which pulls values
# from HashiCorp Vault at path: secret/data/banking/mongodb
#
# DO NOT add plaintext credentials here — this file is tracked in git.
# See manifests/security/external-secrets/cluster-secret-store.yaml for
# the Vault → ESO bridge configuration.
